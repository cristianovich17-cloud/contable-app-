# ============================================================
# CONTABLE APP - ENVIRONMENT VARIABLES TEMPLATE
# ============================================================
# Copy this file to .env.local for development
# or configure on deployment platform for production
# ============================================================

# ============================================================
# 1. DATABASE CONFIGURATION
# ============================================================

# Development (SQLite)
# DATABASE_URL="file:./prisma/dev.db"

# Production (PostgreSQL)
# Ejemplo: postgresql://user:password@host:5432/dbname
DATABASE_URL=postgresql://user:password@localhost:5432/contable_db

# ============================================================
# 2. AUTHENTICATION (JWT)
# ============================================================

# IMPORTANTE: 
# - Para development, usar valor por defecto es OK
# - Para production, DEBE ser una cadena larga y aleatoria
# - Generar con: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"

JWT_SECRET=tu-secret-key-super-seguro-2025

# ============================================================
# 3. EMAIL CONFIGURATION (SMTP)
# ============================================================

# SMTP Server (Gmail por defecto)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587

# Gmail: Habilitar "Contraseñas de aplicación"
# https://myaccount.google.com/apppasswords
SMTP_USER=tu-email@gmail.com
SMTP_PASSWORD=xxxx-xxxx-xxxx-xxxx

# Email de origen
SMTP_FROM_EMAIL=tu-email@gmail.com
SMTP_FROM_NAME=Contable App

# ============================================================
# 4. REDIS CONFIGURATION (para Queue de emails)
# ============================================================

# Redis para BullMQ
REDIS_HOST=127.0.0.1
REDIS_PORT=6379
# REDIS_PASSWORD=opcional-si-redis-tiene-autenticacion

# ============================================================
# 5. NEXT.JS CONFIGURATION
# ============================================================

# Node environment
NODE_ENV=development

# API URL (para requests desde cliente)
# NEXT_PUBLIC_API_URL=http://localhost:3000

# ============================================================
# NOTAS IMPORTANTES
# ============================================================

# Para development:
# 1. Copiar este archivo a .env.local
# 2. Actualizar SMTP_USER y SMTP_PASSWORD con credenciales de Gmail
# 3. Ejecutar: npm run dev

# Para production (Vercel):
# 1. Ir a Project Settings > Environment Variables
# 2. Agregar todas las variables (excepto DATABASE_URL que va en Database tab)
# 3. Cambiar JWT_SECRET a valor seguro
# 4. Cambiar DATABASE_URL a PostgreSQL production

# Para production (Fly.io):
# 1. Crear secrets: fly secrets set VAR_NAME="value"
# 2. Vincular PostgreSQL addon
# 3. Vincular Redis addon
# 4. Desplegar: fly deploy

# ============================================================
